import React from "react";
import StreamsPage from "../../../components/admin/streams";

const Streams = () => {
  return (
    <div>
      <StreamsPage />
    </div>
  );
};

export default Streams;
